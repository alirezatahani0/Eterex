# Site Configuration
# The full URL of your site (used for SEO, sitemap, canonical URLs)
# Required for production
NEXT_PUBLIC_SITE_URL=https://eterex.com

# Base Path (optional)
# If deploying to a subdirectory, set this (e.g., "/app" or "/eterex")
# Leave empty if deploying to root domain
# Example: NEXT_PUBLIC_BASE_PATH=/eterex
NEXT_PUBLIC_BASE_PATH=

# Asset Prefix (optional)
# If using a CDN for static assets, set this (e.g., "https://cdn.eterex.com")
# Leave empty to use same domain
# Example: NEXT_PUBLIC_ASSET_PREFIX=https://cdn.eterex.com
NEXT_PUBLIC_ASSET_PREFIX=

# API Configuration
# Main API base URL for market data
# Default: https://api.eterex.com/api
NEXT_PUBLIC_API_URL=https://api.eterex.com/api

# Public API base URL for assets and prices
# Default: https://publicv2.eterex.com/public/api
NEXT_PUBLIC_PUBLIC_API_URL=https://publicv2.eterex.com/public/api

# Google Search Console Verification (optional)
# Add your Google verification code here for Search Console
# Example: GOOGLE_VERIFICATION=abc123def456
GOOGLE_VERIFICATION=

# Server Configuration
# Port for the Next.js server (default: 3000)
# Only needed if running locally, Docker uses PORT from docker-compose
PORT=3000

# Node Environment
# Automatically set to 'production' in Docker builds
# For local development, Next.js handles this automatically
# NODE_ENV=development
